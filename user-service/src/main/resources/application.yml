server:
  port: 8092

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3307/demo

  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: USER-SERVICE



eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost